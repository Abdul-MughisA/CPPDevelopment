#include <iostream>
#include "Header.h" // includes file in same directory which in turn links function in another file

int main()
{
    std::cout << Function(); // this is the function wished to be called
}
